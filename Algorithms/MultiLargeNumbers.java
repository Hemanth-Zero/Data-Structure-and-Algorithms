public class MultiLargeNumbers {
    public void algorithm(){
            /*      
                stoi(Sting A) return Integer A ;
                MultiLargeNumbers(String A,String B){
                    if( |A| < 5 && |b| < 5) 
                        return  (stoi(A)*stoi(b)).toString()
                    n = max(|A|,|B|)
                    m = m/2
                    a,b = split(A,m)
                    c,d = split(B,m)
                    P0 = MultiLargeNumbers(a,c)
                    P2 = MultiLargeNumbers(b,d)
                    P1 = MultiLargeNumbers(stradd(a,b),stradd(c,d))

                    return Shift(P0,2m)+Shift(P1-P0-P2,m)+P2
                }
            */
    }
}