public class ActivitySelection {
    public void algorithm(){
        /*
            ActivitySelection(Activity[] activites ){
                Sort(activites, finishtime)  -> nlogn
                printf(activites[0])
                prev = activites[0].finishtime  
                for(int i = 1;i<activites.length;i++){  -> n
                        if(activites[i].start >= prev)
                            printf(activites[i])
                            prev = activites[i].finishtime
                }
            }

            Total complexity = O(n logn)
        */
    }
}